schema: xsd/NeTEx_publication.xsd
logLevel: debug # default is undefined, setting this parameter disables the fancy setting, regardless of its value
fancy: true
inputs:
  - ~/.greenlight/documents
  - /etc/greenlight/documents
  - /documents
  - /greenlight/documents
  - ./documents
outputs:
  - report: # logged in standard output
      format: mdext # mdext (markdown extended) or mds (markdown simple)
  - file:
      format: json # formats available are: json or csv
      path: /greenlight # where to save the file (filename format is ${path}/report-${current_date_time}.${format}
disableBuiltinScripts: false
scripts:
  - ~/.greenlight/scripts
  - /etc/greenlight/scripts
  - /scripts
  - /greenlight/scripts
  - ./scripts
